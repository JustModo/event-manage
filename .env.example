# AWS S3 Configuration
S3_BUCKET=eventsphere-uploads
S3_REGION=ap-south-1
CLOUDFRONT_DOMAIN=

# AWS Auth: attach an IAM Instance Role to your EC2 — the SDK picks up credentials automatically

# Ports
PORT=3000
BACKEND_PORT=3000
WEB_PORT=80

# URLs (customize for your EC2 / domain setup)
BACKEND_URL=http://your-ec2-ip:3000
